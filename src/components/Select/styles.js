import styled from 'styled-components/native';
import Colors from '../../constants/Colors';

export const Container = styled.View`
  margin: 0 auto;
  border: 2px solid ${Colors.tintColor};
  background:#fff;
  padding-left: 10px;
  border-radius: 5px;
  justify-content: center;
  height: 50px;
  width:90%;
  margin-top: 5px;
`;
